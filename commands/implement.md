---
description: Implement an approved spec stage by stage
---

Begin implementing an approved spec, keeping the spec the canonical source of
truth. Invoke the `specforge:implement-spec` skill. Spec path or name:

$ARGUMENTS
