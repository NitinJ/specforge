---
description: Review a spec and file the review as its child spec
---

Review a SpecForge spec as a reviewer agent. Invoke the
`specforge:write-review` skill. Spec to review (id or title):

$ARGUMENTS
