---
description: Process a submitted batch of spec comments (reply + amend)
---

Process the submitted comment batch for a spec: reply inline to each thread and
amend the spec. Invoke the `specforge:review-spec` skill. Spec path or batch id
(optional):

$ARGUMENTS
