---
description: Export a spec as a GitHub-flavoured markdown file (+ its diagrams)
---

Render a spec from the store as markdown that opens correctly on GitHub, plus a
sidecar directory holding its diagrams. Invoke the `specforge:export-md` skill.
Spec id (or a description of which spec, or a destination path):

$ARGUMENTS
