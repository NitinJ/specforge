---
description: Open a spec in the SpecForge browser review server
---

Start (or focus) the SpecForge review server and open a spec for review. Invoke
the `specforge:serve-spec` skill. Spec path or name (optional):

$ARGUMENTS
