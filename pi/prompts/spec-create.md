---
description: Author a new house-style SpecForge spec (.html)
argument-hint: "<topic / feature / bug to spec>"
---

Create a new design spec using SpecForge house rules. Load and follow the
`specforge:create-spec` skill (e.g. `/skill:specforge:create-spec`, or read its
SKILL.md under the specforge package's `skills/create-spec/`). Topic / feature /
bug to spec:

$ARGUMENTS
