---
description: Convert an existing .md/.html design doc into a SpecForge spec
argument-hint: "<file to convert>"
---

Convert an existing design/spec file into a SpecForge spec in the store (ingest
HTML as-is, or re-author markdown/freeform into a house-style spec), attached to
this session. Load and follow the `convert-spec` skill (e.g.
`/skill:convert-spec`, or read its SKILL.md under the specforge
package's `skills/convert-spec/`). File to convert:

$ARGUMENTS
