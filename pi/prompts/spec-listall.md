---
description: List all SpecForge specs in the store (id · title · status · attached)
---

List every SpecForge spec in the store with its status and which session (if any)
has it attached, plus the index URL to open in the browser. Load and follow the
`specforge:list-specs` skill in "all" mode (`/skill:specforge:list-specs`, or
read its SKILL.md under the specforge package's `skills/list-specs/`).
