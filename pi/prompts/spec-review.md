---
description: Review a SpecForge spec and file the review as its child spec
argument-hint: "<spec id or title to review>"
---

Review a SpecForge spec as a reviewer agent. Load and follow the
`write-review` skill (e.g. `/skill:write-review`, or read its SKILL.md under
the specforge package's `skills/write-review/`). Spec to review:

$ARGUMENTS
