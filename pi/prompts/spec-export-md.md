---
description: Export a spec as a GitHub-flavoured markdown file (+ its diagrams)
argument-hint: "<spec id / description / destination path>"
---

Render a spec from the store as markdown that opens correctly on GitHub, plus a
sidecar directory holding its diagrams. Load and follow the
`specforge:export-md` skill (e.g. `/skill:specforge:export-md`, or read its
SKILL.md under the specforge package's `skills/export-md/`). Spec id (or a
description of which spec, or a destination path):

$ARGUMENTS
